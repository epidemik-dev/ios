# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Epidemik' do
  platform :ios, '10.0'
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Epidemik
  pod 'SwiftyJSON', '~> 4.0'

  target 'EpidemikTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'EpidemikUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'EpidemikWidget' do
  platform :ios, '10.0'
  pod 'SwiftyJSON', '~> 4.0'
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for EpidemikWidget

end
